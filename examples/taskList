# usage: ./taskList <tes-endpoint> <token>
# example: ./taskList https://tes-ebrains.apps-dev.hbp.eu $TOKEN

baseUrl=$1
token=$2

curl -i -X GET "$baseUrl/ga4gh/tes/v1/tasks?view=FULL" --header "Authorization: Bearer $token"
